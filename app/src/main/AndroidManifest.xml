<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="${suntimesPermissionRoot}.permission.READ_CALCULATOR" />  <!-- required to interact w/ Suntimes -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuntimesMapPack"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="suntimes.action.ADDON_MENUITEM" />
                <category android:name="suntimes.SUNTIMES_ADDON" />
            </intent-filter>
            <meta-data android:name="SuntimesMenuItemTitle" android:value="@string/app_name" />

            <intent-filter>
                <action android:name="suntimes.action.ADDON_WORLDMAP_BACKGROUND" />
                <category android:name="suntimes.SUNTIMES_ADDON" />
            </intent-filter>
            <meta-data android:name="WorldMapBackgroundProvider"
                android:value="content://${mappackAuthorityRoot}.provider" />

        </activity>

        <provider
            android:name="com.forrestguice.suntimes.mappack.SuntimesMapProvider"
            android:authorities="${mappackAuthorityRoot}.provider"
            android:exported="true" android:permission="${suntimesPermissionRoot}.permission.READ_CALCULATOR"
            android:syncable="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${mappackAuthorityRoot}.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

    </application>

    <queries>
        <package android:name="${suntimesApplicationId}" />    <!-- package visibility api 30+ -->
    </queries>

</manifest>